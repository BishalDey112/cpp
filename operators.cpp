#include<iostream>
using namespace std;

int main(){
    float a = 5.4; int b =4;
    // cout << a << endl;
    // cout << (++a) << endl;//  pre increment pehle increment karo uske baad use karlena
    // cout << (a++) << endl;// increment toh hoga but print next line me hoga increment wala
    // cout << a << endl; //idhar increment wala print hoga
    // cout << (--a) << endl;// idhar its pre decrement same like pre increment
    // cout << (a--) << endl; // same like post decrement
    // cout << a;

    // ~ idhar iss operator ko tilda bola jata hai 
    // ab iske bare mein bhi thora jaan lete hai chalo 
    // jab bhi hum tilda ~ isko koi number dete hai wo usko flip kar deta hai(gazab ulta)
    // cout << (a&b) << endl;

    // cout << (a|b) << endl;

    cout << ~1 << endl;// 5 is 0101~ tilda hoga 1010 that is -6(simple bolne se 1 hone se tilda -2 hoga then agar 5 thn -6 itna socho mat bhailog)
    cout << (5^5) << endl; // isko zor bolte hai bhailog ^
    // cout << a % 2 << endl;
    return 0;
}